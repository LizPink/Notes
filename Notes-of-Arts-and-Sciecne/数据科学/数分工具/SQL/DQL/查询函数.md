# 工具函数
## 序列函数
在POSTGRE中使用`GENERATE_SERIES`函数可以生成一列数值或者日期序列。

# 日期函数
在POSTGRE中可以使用`DATE_TRUNC`函数来获取DATE/TIMESTAMP对应的标准周度月度年度数据。

# 文本函数
在POSTGRE中使用`STRING_AGG`函数可以将多行数字连成一个字符串类型。
```POSTGRE
STRING_AGG(expr, delimiter)
```
