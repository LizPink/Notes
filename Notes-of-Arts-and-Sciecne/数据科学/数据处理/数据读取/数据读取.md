# CSV File
通常编写一个程序来读取CSV文件，可以采用字典列表的方式读取CSV文件。
```python
def parse_csv(filepath, header=True, delimiter=',') -> list[dict]:
    """
    Parse a CSV file into lisf of dict.
    """
    output = []
    headers = file.readline().strip().split(delimiter)
    for line in file:
        row_data = {}
        line = line.strip().split(delimiter)

        for col, fields in enumerate(line):
            header = headers[col].strip()
            fields = fields.strip()
            row_data[header] = fields
        output.append(row_data)

    return output
```