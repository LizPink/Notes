枚举类型是整数类型的一个重要应用。枚举类型之间的一个重要特性就是可以类型内部比较大小，这在实际工程中可以用来表示各种重要性不同的状态。枚举类型的成员均是编译期常量。

枚举类型的定义如下，通过类定义的枚举类型不再能隐式转换为整型。

```cpp
// Example1
enum StatusClass
{
	START,
	STOP,
	FATAL
};

// Example2
enum class StatusClass
{
	START,
	STOP,
	FATAL
};
```