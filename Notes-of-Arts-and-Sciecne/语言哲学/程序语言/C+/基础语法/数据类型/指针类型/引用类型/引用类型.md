引用类型必须初始化，并且在初始化之后就不能再进行赋值，也就是说这种绑定是永久性的。

# 指针的定义
## 函数指针
```cpp
/* 函数指针的声明 */
// 等价写法
返回类型(*ptr)(参数类型);

// 等价写法
using Functype = 返回类型 (参数类型);
FuncType* ptr;

// 等价写法
using FuncType = 返回类型(*)(参数类型);
FuncType ptr;
```