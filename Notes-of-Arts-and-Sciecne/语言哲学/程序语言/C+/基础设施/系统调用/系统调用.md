在C++中可以通过`std::system`实现与操作系统的命令交互。在命令执行时，程序会查找环境变量中的命令行解释器，并将对应的命令传递给命令行解释器。
```cpp
std::system(const char* command)
```

```cpp
// Example1
std::system("echo hello");
std::system("pwsh -command echo hello");
```