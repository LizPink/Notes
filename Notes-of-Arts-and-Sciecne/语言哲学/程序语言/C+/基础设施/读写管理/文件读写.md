这一节我们来介绍C++文件读写的基本内容，我们会涵盖基本的函数与接口。
# 文件读取
在C++中进行文件的读取的模块称为std::ifstream模块，该模块采用类与对象的设计思维，通过实例化对象抽象文件的读写过程。
```C++
/* std::ifstream */
std::ifstream(
	filepath			# filepath: std::string&
)
```


# 文件写入