当一个类内部实现了`__enter__()`和`__exit()__`方法后，就可以通过`with`语句进行过程管理。当该语句执行时`with`会先调用obj对象的`__enter__()`方法，随后在语句执行完成后自动调用`__exit()__`方法。
```python
# with-clause
with obj as o:
	pass
	
## Example1
### 文件读取过程
with open(filepath) as f:
	pass
```