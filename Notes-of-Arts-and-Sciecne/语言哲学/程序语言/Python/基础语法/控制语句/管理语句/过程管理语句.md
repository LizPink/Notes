当一个类内部实现了`__enter__()`和`__exit()__`方法后，就可以通过`with`语句进行过程管理。在代码块执行前会先调用对象的`__enter__()`方法，在代码块执行完成后自动调用对象的`__exit()__`方法。
```python
# with-clause
with obj as o:
	pass
	
## Example1
### 文件读取过程
with open(filepath) as f:
	pass
```